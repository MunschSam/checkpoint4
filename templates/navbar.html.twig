<nav class="uk-navbar-container uk-margin" uk-navbar>
    <div class="uk-navbar-center">

        <div class="uk-navbar-center-left"><div>
            <ul class="uk-navbar-nav">
            <li><a uk-icon="home" href="{{ path('home') }}"></a></li>
            <li><a href="{{ path('hotel_index') }}">Hotel</a></li>
            <li><a href="{{ path('restaurant_index') }}">Restaurant</a></li>
            </ul>
        </div></div>
        <div class="uk-navbar-item uk-logo" uk-icon="icon: world; ratio: 2"></div>
        <div class="uk-navbar-center-right"><div>
            <ul class="uk-navbar-nav">
                <li><a href="{{ path('blog_index') }}">Blog</a></li>
            </ul>
            {% if app.user %}
    {{ app.user.email }}
    <a href="{{ path('app_logout') }}"> Se déconnecter</a>
{% else %}
    <a href="{{ path('app_login') }}"> Se connecter</a>
{% endif %}
        </div></div>

    </div>
</nav>