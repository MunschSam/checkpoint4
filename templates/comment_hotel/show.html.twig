{% extends 'base.html.twig' %}

{% block title %}CommentHotel{% endblock %}

{% block body %}
    <h1>CommentHotel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment_hotel.id }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ comment_hotel.comment }}</td>
            </tr>
            <tr>
                <th>CommentDate</th>
                <td>{{ comment_hotel.commentDate ? comment_hotel.commentDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('comment_hotel_index') }}">back to list</a>

    <a href="{{ path('comment_hotel_edit', {'id': comment_hotel.id}) }}">edit</a>

    {{ include('comment_hotel/_delete_form.html.twig') }}
{% endblock %}
