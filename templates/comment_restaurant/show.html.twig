{% extends 'base.html.twig' %}

{% block title %}CommentRestaurant{% endblock %}

{% block body %}
    <h1>CommentRestaurant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment_restaurant.id }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ comment_restaurant.comment }}</td>
            </tr>
            <tr>
                <th>CommentDate</th>
                <td>{{ comment_restaurant.commentDate ? comment_restaurant.commentDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('comment_restaurant_index') }}">back to list</a>

    <a href="{{ path('comment_restaurant_edit', {'id': comment_restaurant.id}) }}">edit</a>

    {{ include('comment_restaurant/_delete_form.html.twig') }}
{% endblock %}
